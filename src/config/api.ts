// Único lugar a tocar cuando llegue la API real de la cátedra: cambiar
// EXPO_PUBLIC_API_URL en .env (o .env.local) por la URL definitiva.
export const API_URL = process.env.EXPO_PUBLIC_API_URL ?? 'http://localhost:3001';
