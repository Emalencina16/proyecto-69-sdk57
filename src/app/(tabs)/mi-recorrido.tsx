import { Text, View } from 'react-native';

export default function MiRecorrido() {
  return (
    <View>
      <Text>Mi recorrido</Text>
      <Text>Pendiente: visitas registradas (QR, GPS o manual) con foto y nota (src/servicios/visitas.ts).</Text>
    </View>
  );
}
